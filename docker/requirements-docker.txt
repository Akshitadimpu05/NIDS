# Minimal Docker Requirements for NIDS Deployment
# Based on actual imports in the codebase

# Core Python packages
numpy>=1.21.0
pandas>=1.5.0

# Deep Learning (PyTorch ecosystem)
torch>=1.12.0,<2.0.0
scikit-learn>=1.0.2

# Web Framework and API
fastapi>=0.95.0
uvicorn[standard]>=0.21.1
aiohttp>=3.8.4
requests>=2.28.0

# Data processing and serialization
pyyaml>=6.0
python-multipart>=0.0.6
joblib>=1.2.0

# Async and networking
websockets>=10.4

# Monitoring and metrics
prometheus-client>=0.16.0
psutil>=5.9.0

# Logging
loguru>=0.6.0

# Network analysis (minimal for traffic capture)
scapy>=2.4.5

# RL environment (gymnasium for gym compatibility)
gymnasium>=0.28.0

# Time and utilities
python-dateutil>=2.8.2
tqdm>=4.64.0

# Additional dependencies found in codebase
pydantic>=1.10.0
python-dotenv>=1.0.0
